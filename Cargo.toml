[package]
name = "macromaybe"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "^4.0", features = ["derive", "string"] }
seq-macro = "0.3"
serde = { version = "1.0", features = ["derive"] }
